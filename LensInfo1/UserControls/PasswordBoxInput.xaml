<UserControl x:Class="LensInfo1.UserControls.PasswordBoxInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LensInfo1.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="150">
    <Grid>
        <PasswordBox x:Name="InputPasswordBox" Background="Transparent" BorderBrush="BurlyWood" PasswordChanged="InputPasswordBox_PasswordChanged" VerticalAlignment="Center" BorderThickness="2" FontSize="20" Margin="5" />
        <TextBlock x:Name="PasswordGuide" VerticalAlignment="Center" HorizontalAlignment="Left" Panel.ZIndex="-1" FontWeight="Light" FontStyle="Italic" Foreground="Gray" Margin="5" FontSize="18"/>
        <Button x:Name="ButtonClear" Foreground="BurlyWood" Background="Transparent" BorderThickness="0" Margin="0,0,10,0" VerticalAlignment="Center" HorizontalAlignment="Right" Content="X"/>
    </Grid>
</UserControl>
