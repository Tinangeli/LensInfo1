<UserControl x:Class="LensInfo1.UserControls.InputTextBoxTime"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LensInfo1.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="90">
    <Grid>
        <TextBox x:Name="TextInput" Margin="5" Background="Transparent" VerticalAlignment="Center" FontSize="20" FontWeight="Normal" FontStyle="Normal" TextChanged="TextInput_TextChanged" BorderBrush="BurlyWood"/>

        <TextBlock x:Name="TextGuide" Margin="8" Background="Transparent" Foreground="#FFA59887" VerticalAlignment="Center" FontSize="20" FontWeight="Light" FontStyle="Italic"
           Panel.ZIndex="-1"/>
        <Button x:Name="btnClear" Background="Transparent" HorizontalAlignment="Right" Width="50" VerticalAlignment="Center" Content="X" Click="btnClear_Click" BorderThickness="0" Foreground="BurlyWood" Margin="5" />

    </Grid>
</UserControl>
